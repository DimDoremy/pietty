pietty 任务清单

== MVP（已完成）==

[T] Task 1  项目脚手架与依赖
[T] Task 2  UTF-8 增量解码器
[T] Task 3  pyte 屏幕状态 → Rich Text（含颜色）
[T] Task 4  布局结构
[T] Task 5  TerminalWidget（PTY + pyte + 渲染 + resize + 输入）
[T] Task 6  PiettyApp 组装
[T] Task 7  裸 shell ANSI/UTF-8 验收

== 功能增强（已完成）==

[DONE] config.toml 加载 + theme 解析
[DONE] niri 风格 tab 栏（j/k 切换 tab, h/l 切换 shell）
[DONE] 概览模式（模态窗口，进程名显示，跳转/关闭/标记/移动）
[DONE] ; 前缀（Alt 替代方案）
[DONE] 网格布局（tab/shell 二维管理, Alt+j/k 头插尾插新 tab）
[DONE] 关闭确认（有运行中进程时提示 k/b/escape）
[DONE] 渲染优化（Style 缓存 + GC 调优）
[DONE] README 文档

== 冻结状态（不在核心范围内，如需要作为插件实现）==

[PLUGIN] 文件树侧边栏 + cwd 跟随（/proc/<pid>/cwd）
[PLUGIN] 快捷键提示 footer（? 切换）
[PLUGIN] 后台 job 托管（注入 trap，自托管子进程到新 tab）
[PLUGIN] 桌面感知（检测 DISPLAY，配色作用于 theme）
[PLUGIN] 真并列排版（按 ratio 用 Horizontal/Vertical 容器重建）
